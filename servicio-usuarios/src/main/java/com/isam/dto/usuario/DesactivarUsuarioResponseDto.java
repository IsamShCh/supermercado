package com.isam.dto.usuario;

/**
 * DTO para la respuesta de desactivación de usuario.
 * Respuesta vacía en caso de éxito según el proto.
 * Coincide con DesactivarUsuarioRequest.Response del proto.
 */
public record DesactivarUsuarioResponseDto(
) {}